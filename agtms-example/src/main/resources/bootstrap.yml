spring:
  application:
    name: EXAMPLE
  cloud:
    zookeeper:
      connect-string: zookeeperserver:2181

ribbon:
  eager-load:
    enabled: true

server:
  port: 7899
  compression:
    enabled: true
const state = {
    template: {
        
    }
};

const mutations = {
    setTemplate(state, template) {
        if (template) {
            state.template = template;
        }
    }
};

const actions = {
    
};

export default {
    state,
    mutations,
    actions
}
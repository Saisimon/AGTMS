spring:
  application:
    name: AGTMS-WEB
  mvc:
    dispatch-options-request: true
  main:
    banner-mode: 'off'
  servlet:
    multipart:
      max-file-size: 10MB
  jpa:
    show-sql: true
    generate-ddl: true

server:
  port: 7892
  compression:
    enabled: true

white:
  list:
    urls: /user/auth
  prefix:
    urls: /image/res
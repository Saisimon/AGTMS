spring:
  main:
    banner-mode: 'off'
  servlet:
    multipart:
      maxFileSize: 20MB
      maxRequestSize: 100MB
  jackson:
    default-property-inclusion: non_null
  jpa:
    generateDdl: true
    
logging:
  level:
    root: error